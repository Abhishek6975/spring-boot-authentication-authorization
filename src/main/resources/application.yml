spring:
  application:
    name: authentication-service
  profiles:
    active: test

server:
  port: 8080

springdoc:
  swagger-ui:
    enabled: ${SWAGGER_ENABLED:false}
  api-docs:
    enabled: ${SWAGGER_ENABLED:false}